FROM rust:1.97-bookworm AS builder

WORKDIR /src
COPY Cargo.toml Cargo.lock ./
COPY crates/ crates/

RUN cargo fetch
RUN cargo build --release --package tunnet-edge \
    && strip target/release/tunnet-edge

FROM debian:bookworm-slim

RUN apt-get update \
    && apt-get install -y --no-install-recommends ca-certificates \
    && rm -rf /var/lib/apt/lists/*

COPY --from=builder /src/target/release/tunnet-edge /usr/local/bin/tunnet-edge

EXPOSE 443 80

ENTRYPOINT ["tunnet-edge"]
CMD ["run"]
